import React from 'react';
import './index.pcss';

const Empty = (props) => (
    <div className="empty-wrapper">
        <i className="material-icons">sentiment_dissatisfied</i> ops no data
    </div>
);

export default Empty;